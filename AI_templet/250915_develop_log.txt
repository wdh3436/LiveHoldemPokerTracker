오늘의 개발 내역 (250915):

*   **HUD 기능 강화 (WMSD, WTSD 스탯 추가)**
    *   **기획서 ID:** 3-1-1 (저장된 플레이어 스테이터스)

*   **데이터 모델 수정 (`PlayerProfile.kt`)**
    *   `wentToShowdownCount` (쇼다운까지 간 횟수) 필드 추가.
    *   `wonAtShowdownCount` (쇼다운에서 이긴 횟수) 필드 추가.
    *   `getWmsd()`, `getWtsd()`: 위 데이터를 기반으로 WMSD(쇼다운 진출률), WTSD(쇼다운 승률)를 계산하는 함수 추가.

*   **뷰모델 로직 구현 (`SessionViewModel.kt`)**
    *   **승자 선택 로직 추가:**
        *   `showWinnerSelection` 상태 변수를 추가하여, 핸드 종료 시 승자 선택 UI를 표시하도록 제어.
        *   `selectedWinners` 리스트를 추가하여 무승부 시 다중 승자를 선택할 수 있도록 구현.
        *   `onWinnerSelected()`: 플레이어를 승자 목록에 추가/제거하는 함수 구현.
        *   `confirmWinnersAndStartNewHand()`: '승자 확정' 시 WMSD 및 WTSD 스탯을 최종 계산하고 다음 핸드를 시작하는 핵심 로직 구현.
    *   **핸드 종료 로직 수정:** `nextPhase`, `endBettingRound`, `handleAction` 함수를 수정하여, 폴드로 끝나거나 리버까지 진행되는 모든 핸드 종료 상황에서 승자 선택 로직이 실행되도록 트리거를 설정.

*   **UI 구현 및 개선 (`CurrentSessionScreen.kt`)**
    *   **1차 구현 (팝업 다이얼로그 방식):**
        *   핸드 종료 시 중앙에 `WinnerSelectionDialog` 팝업창을 띄워 승자를 선택하는 방식 구현.
    *   **2차 리팩토링 (통합 UI 방식):**
        *   사용자 피드백을 반영하여 팝업 방식을 제거.
        *   핸드가 종료되면 기존 액션 버튼(폴드, 콜 등)을 숨기고 '승자 확정' 버튼으로 교체.
        *   폴드하지 않은 플레이어의 좌석(`PlayerSeat`)을 직접 터치하여 승자를 선택하는 방식으로 변경.
        *   선택된 플레이어는 테두리 색상(하늘색)으로 강조 표시.
    *   **HUD 업데이트:**
        *   게임 중 각 플레이어 좌석에 표시되는 실시간 HUD에 WMSD, WTSD 스탯을 추가.

*   **프로필 화면 UI 수정 (`ProfileScreen.kt`)**
    *   플레이어의 상세 정보 화면에 WMSD, WTSD 스탯이 표시되도록 UI 업데이트.