오늘까지의 진행 상황 요약 (250821):

*   **프로필 기능 대규모 강화 완료:**
    *   **프로필 리스트 개선:** 기존의 이름만 표시되던 리스트를 개선하여, 각 프로필의 핵심 통계(VPIP, PFR)가 함께 표시되도록 변경.
    *   **프로필 상세 정보 화면 구현:** 리스트에서 프로필을 선택하면 상세 통계, 메모, 태그를 모두 확인할 수 있는 전용 상세 화면으로 이동하도록 구현.
    *   **메모 기능 추가:** 프로필별로 자유로운 길이의 텍스트 메모를 작성하고 저장, 수정할 수 있는 기능 추가.
    *   **태그 시스템 구현:**
        *   이름과 색상을 가진 태그를 동적으로 생성하는 기능 구현.
        *   프로필에 여러 개의 태그를 추가하거나 제거하는 기능 구현 (다대다 관계 설정).
        *   상세 화면 내 팝업(Dialog)을 통해 직관적인 태그 관리 UI 제공.
*   **데이터베이스 리팩토링 및 오류 수정:**
    *   메모 및 태그 기능 추가에 따라 데이터베이스 스키마를 대폭 수정 (테이블 추가, 관계 설정).
    *   변경된 스키마에 맞춰 DAO와 ViewModel 로직을 전면 재구성하고, 이 과정에서 발생한 빌드 오류 해결.

**다음 개발을 위해 기억할 내용:**

*   프로필 데이터의 안정적인 보존을 위한 **데이터 백업 및 복원 기능** 구현 논의 필요.
*   `ProfileListScreen`에 태그를 표시하여 가시성 증대 고려.

--- 2025년 8월 21일 작업 내역 ---

*   **빌드 오류 해결:**
    *   KSP `unexpected jvm signature V` 오류 해결 (Room 2.6.1 -> 2.7.0 업데이트).
    *   `ProfileScreen.kt`의 `Tag` 및 `LazyRow` 임포트 누락 오류 해결.
    *   `SessionViewModel.kt`의 `insertOrUpdate` 함수명 오타 수정.
    *   아이콘 참조 오류 해결 (명시적 임포트 및 캐시 문제).
*   **태그 기능 개선:**
    *   **`ProfileListScreen`에 태그 표시 기능 구현:**
        *   `PlayerProfileDao`에 `getAllProfilesWithTags()` 메소드 추가.
        *   `ProfileViewModel`에서 `profiles`를 `ProfileWithTags` 리스트로 제공하도록 수정.
        *   `ProfileListScreen`에서 `ProfileWithTags`를 사용하여 태그 표시.
    *   **태그 정의 삭제 기능 구현:**
        *   `PlayerProfileDao`에 `deleteTag(tag: Tag)` 메소드 추가.
        *   `ProfileViewModel`에 `deleteTag(tag: Tag)` 함수 추가.
        *   `AddTagDialog`에 태그 정의 삭제 버튼 추가.
    *   **태그 UI 개선:**
        *   `AddTagDialog`에서 태그 옆에 두 개의 'X' 아이콘이 표시되는 문제 해결 (`TagChip`에 `showRemoveIcon` 파라미터 추가).
        *   태그 정의 삭제 아이콘을 'X' (`Icons.Default.Close`)로 변경.
    *   **태그 색상 적용 문제 해결:**
        *   `Tag` 데이터 클래스의 `tagColor` 타입을 `Int`에서 `Long`으로 변경.
        *   `ProfileViewModel`에서 `Color.value.toLong()`으로 색상 값 저장.
        *   `TagChip`에서 `Color(tag.tagColor.toULong())`으로 색상 표시.
*   **디버깅 로그 제거.**
